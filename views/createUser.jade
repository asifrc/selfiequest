extends layout

block content
  h1= title
  form#createUserForm(action="/admin/createUser", method="POST", data-ajax="false")
    input#name(type="text", name="firstName", placeholder="first name")
    input#name(type="text", name="lastName", placeholder="last name")
    input#email(type="email", name="email", placeholder="e-mail address")
    input(type="submit" value="Save")
