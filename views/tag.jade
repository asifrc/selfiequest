extends layout

block content
  h1= title
  img(src="#{imgPath}", width="100%")
  form(action="/save", method="POST", data-ajax="false")
    input(type="text", name="tagged", placeholder="Tag a TWer!")
    input(type="submit" value="Save")
